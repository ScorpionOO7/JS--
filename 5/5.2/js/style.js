"use script";

/*Взгляните на следующий код:

let str = "Привет";

str.test = 5;

alert(str.test);
Как вы думаете, это сработает? Что выведется на экран?*/

// для примитивного типа создается временный объект не для передачи значений, выдаст ошибку.

let x = 255..toString(2);
alert(x);