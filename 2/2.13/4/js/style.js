"use script";

//При помощи цикла for выведите чётные числа от 2 до 10.

for (let userNumber = 2; userNumber <= 10 ; userNumber++) {
  if (userNumber % 2 == 0) {
    alert (userNumber);
  } 
}